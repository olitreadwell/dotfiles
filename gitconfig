[core]
	excludesfile = ~/.gitignore
[user]
	name = olitreadwell
	email = olitreadwell@gmail.com
[alias]
	co-pr = "! f() { git fetch origin pull/$1/head:pull-request-$1 && git checkout pull-request-$1; }; f"
[credential]
	helper = osxkeychain
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
